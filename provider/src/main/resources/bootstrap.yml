spring:
  application:
    name: provider
  cloud:
    zookeeper:
      connect-string: 192.168.110.128:2185
      discovery:
        register: true
    config:
      profile: dev
      discovery:
        service-id: config
        enabled: true